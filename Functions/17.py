# 17. Write a Python program to find if a given string starts with a given character
# using Lambda.


# (VS Code automatically changed lamba function to a normal function so have to comment the code to save.)

# Code

# check_startswith = lambda string, start_char:True if string[0].upper() == start_char.upper() else False
# print(check_startswith('james', 's'))
